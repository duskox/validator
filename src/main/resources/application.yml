spring:
  datasource:
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1;DATABASE_TO_UPPER=false;INIT=CREATE SCHEMA IF NOT EXISTS ht_sms_schema
    username: sa
    password: sa
    driver-class-name: org.h2.Driver

flyway:
  url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1;DATABASE_TO_UPPER=false;INIT=CREATE SCHEMA IF NOT EXISTS ht_sms_schema
  user: sa
  password: sa
  schemas: validator_db